[package]
name = "summo-vault"
description = "The Markdown vault: meetings and notes as files the user owns"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
summo-core.workspace = true

# Hashing a relative path into a stable id for a file somebody wrote by hand. Not for integrity —
# `sha2` is what verifies downloads — so speed matters more than the algorithm's pedigree.
blake3.workspace = true
serde.workspace = true
serde_yaml.workspace = true
serde_json.workspace = true
time.workspace = true
tracing.workspace = true

[dev-dependencies]
pretty_assertions.workspace = true
tempfile.workspace = true
